name: flutterinit_cli
description: CLI to generate FlutterInit scaffolds without the web UI.
version: 0.2.1
environment:
  sdk: ">=3.3.0 <4.0.0"

executables:
  flutterinit: flutterinit

dependencies:
  args: ^2.6.0
  archive: ^4.0.7
  crypto: ^3.0.6
  http: ^1.2.2
  http_parser: ^4.1.2
  path: ^1.9.0

dev_dependencies:
  test: ^1.25.8
